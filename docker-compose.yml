services:
  android:
    build: .
    working_dir: /workspace
    volumes:
      - .:/workspace
      - gradle-cache:/home/gradle/.gradle
    environment:
      GRADLE_USER_HOME: /home/gradle/.gradle
    stdin_open: true
    tty: true

volumes:
  gradle-cache:
